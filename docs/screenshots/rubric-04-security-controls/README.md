# Rubric 4 screenshots — NSGs, ASGs, Private Endpoints

Put screenshots for Rubric 4 in this folder.

## Recommended set

- NSG list + at least one NSG rules table (HR is fine)
- ASG list
- Private endpoint list (showing storage PE)
- Private DNS zone + A records
- VM-side validation (`nslookup` showing private IP + `curl -I` response)


## Supporting write-up

See `docs/RUBRIC_04_NSG_ASG_PRIVATE_ENDPOINTS.md`.

